1) git config --global user.email "bha@gmail.com"
git config --global user.name "bhargavi"
gedit g1.txt
git add .
git commit -m "l"
git log

git checkout -b feature-branch
 echo "g2">g2.txt
git add .
git commit -m "p"
git checkout main
git merge feature-branch


2) git branch
git checkout feature-branch
echo "k">g4.txt
git add .
git stash save "mis"
git stash list
git stash show stash@{0}
git checkout master
git stash apply
git stash list
git stash drop
git stash clear

git clone -b main githublink/hi.git (main.txt)


3) git clone -b main githublink/hi.git (main.txt)
cd hi
git pull origin main
ls
gedit main.txt
git add .
git commit -m "p"
git push origin main
git checkout -b slave
git checkout main
git rebase slave
ls
git push origin slave

git checkout -b feature-branch
echo "k">g6.txt
git add .
git commit -m "l"
git checkout main
git merge feature-branch -m "k"
git merge feature-branch --no-f -e


4) git log --oneline
git tag v9.0 c1dd939
git tag -a v3.0 -m "hiiaoi"
git push origin v9.0


git checkout slave
echo "l">g10.txt
git add .
git commit -m "p"
git checkout main
git cherry-pick slave .. main
ls

5)git log --oneline
git show df408fb

git log --author="abc" --since="2025-05-26" --until="2025-06-26"

6)git log -n 5
git log -n 5 --oneline
git revert id

git config --global alias.st status
git config --global alias.lg "log --oneline"
git config --global --list


7)git checkout main
echo "g11">g11.txt
git add .
git commit -m "p"
git checkout slave
echo "g22">g22.txt
git add .
git commit -m "l"
 git checkout main
git merge slave -m "merged"
git log --oneline
git revert -m 1 da53230
git log --oneline

git checkout -b test
echo "te">g13.txt
git add .
git commit -m "p"
git checkout main
 git branch -D test
git reflog
git checkout -b test 09800f3


8) touch g13.txt g14.log g15.md
 mkdir temp
 touch temp/klo
git clean -n

git log --oneline
git archive --format=zip --output=b.zip f55d85f
git archive --format=tar --output=c.tar cd73be0
